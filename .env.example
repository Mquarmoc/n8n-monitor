# Environment Variables for n8n Monitor Android App
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control!

# Google Play Console Service Account
# Option 1: Path to service account JSON file
GOOGLE_PLAY_JSON_KEY_PATH=/path/to/your/service-account.json

# Option 2: Service account JSON content (alternative to file path)
# GOOGLE_PLAY_JSON_KEY_DATA={"type":"service_account","project_id":"your-project",...}

# App Signing (if using environment variables instead of keystore.properties)
# KEYSTORE_PASSWORD=your_keystore_password
# KEY_PASSWORD=your_key_password

# Optional: Custom Android SDK location
# ANDROID_HOME=/path/to/android/sdk

# Optional: Custom Java home
# JAVA_HOME=/path/to/jdk-17

# Optional: Fastlane configuration
# FASTLANE_SKIP_UPDATE_CHECK=1
# FASTLANE_HIDE_GITHUB_ISSUES=1

# Optional: Build configuration
# GRADLE_OPTS=-Xmx4g -XX:MaxMetaspaceSize=512m

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Source the file: source .env (Linux/macOS) or use a .env loader
# 4. Never commit .env to version control!